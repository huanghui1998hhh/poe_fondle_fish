import 'package:flutter/material.dart';

class PoeDbPage extends StatelessWidget {
  const PoeDbPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
